# AI-Driven-Market-Digital-Twins
**TrustWealth** 是一个旨在解决 AI 金融“黑盒信任危机”的综合决策系统。不同于传统的“只给结论”的预测模型，我们通过**验证优先 (Verification-First)** 的架构，确保从原始财报摄取到最终资产配置建议的每一步都具备逻辑一致性和审计透明度。

---

## 🌟 项目核心价值 (Core Value)

在金融领域，一个“看起来正确”的答案如果缺乏验证过程，就是一种风险。TrustWealth 的迭代核心在于：

* **从“结果驱动”转向“逻辑驱动”**：AI 不再只是“猜”一个数字，而是必须展示其推导逻辑（如计算过程、证据链接）。
* **从“盲目优化”转向“用户对齐”**：确保 AI 的盈利冲动永远不会触碰用户设定的硬性约束底线（如：绝对不碰烟草行业）。

---

## 🏗️ 系统架构与数据流 (System Architecture)

项目采用四层解耦架构，实现了从非结构化数据到可信决策的闭环。

### 数据流向概览

```mermaid
graph TD
    subgraph Phase1_Sensory [感知与基座 - 数据提纯]
        A[原始报表/流水截图] -->|PRM逻辑解析| B(余文湛: 模拟人类分步推理)
        B -->|JSON事实数据| C(陈嘉卓: 语义验证数据库)
    end

    subgraph Phase2_Monitoring [监控与风险 - 外部预警]
        C -->|行情数据| D(邓玉洁: 绘制风险传染地图)
        E[推特/预测市场] -->|噪音过滤| F(袁锦辉: 市场异常审计)
    end

    subgraph Phase3_Strategy [策略生成 - 智慧决策]
        D -->|稳定性信号| G(毛杰: 会反思的交易策略)
        C -->|历史事实| G
    end

    subgraph Phase4_Decision [最终调度 - 私人管家]
        G -->|原始建议| H(江季秀: 守住底线的调度器)
        F -->|异常过滤| H
        I[用户个人约束] -->|硬性边界| H
        H --> J{最终资产配置建议}
    end
